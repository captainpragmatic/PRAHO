# PRAHO Portal Service - Docker Requirements (NO DATABASE ACCESS)
# 🚨 SECURITY: Intentionally excludes database drivers to prevent DB access
# Portal communicates with platform service via API only

# Core Django (minimal)
Django>=5.2,<5.3
django-environ>=0.11.0

# API framework  
djangorestframework>=3.14.0

# HTTP client for platform API communication
requests>=2.31.0

# Production server
gunicorn>=21.2.0

# Session management (use Django's database sessions instead of Redis)
# django-redis-sessions>=0.6.2  # REMOVED: using database sessions

# Security & CSRF
django-cors-headers>=4.3.0

# Logging
structlog>=23.1.0
