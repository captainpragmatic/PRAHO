# Production requirements for PRAHO Platform
# Minimal production-only packages

-r base.txt

# Production WSGI server
gunicorn>=21.2.0
gevent>=23.7.0

# Production database optimizations
django-cachalot>=2.6.0  # ORM query caching
django-redis>=5.4.0     # Redis cache backend

# Monitoring & Performance
newrelic>=8.9.0  # APM monitoring (optional)
datadog>=0.47.0  # Metrics (optional)

# Security enhancements
django-csp>=3.8.0       # Content Security Policy
django-ratelimit>=4.1.0 # Rate limiting

# Task queue production
supervisor>=4.2.0  # Process management

# Romanian legal compliance
reportlab>=4.0.0  # PDF generation for invoices
weasyprint>=60.0  # Alternative PDF generator
